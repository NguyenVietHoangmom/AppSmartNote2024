<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <View
            android:id="@+id/v_view"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginLeft="30dp"
            android:layout_marginRight="30dp"

            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"

            />

        <TextView
            android:id="@+id/tv_doccument"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawableLeft="@drawable/folder"
            android:text="Ghi chú"
            android:textSize="25sp"
            app:layout_constraintBottom_toBottomOf="@id/v_view"
            app:layout_constraintLeft_toLeftOf="@id/v_view"
            app:layout_constraintTop_toTopOf="@id/v_view" />

        <TextView
            android:id="@+id/tv_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="20dp"
            android:text="10"
            android:textSize="25sp"
            app:layout_constraintBottom_toBottomOf="@id/tv_doccument"
            app:layout_constraintRight_toLeftOf="@id/im_right"
            app:layout_constraintTop_toTopOf="@id/tv_doccument" />

        <ImageView
            android:id="@+id/im_right"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/rightwhite"
            app:layout_constraintBottom_toBottomOf="@id/tv_number"
            app:layout_constraintRight_toRightOf="@id/v_view"
            app:layout_constraintTop_toTopOf="@id/tv_number" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>